# Cross-link for embedded recv builds (see scripts/cross-build-aarch64.sh).
# Cross-build uses --no-default-features (headless recv-log); no :arm64 libxcb required.
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
